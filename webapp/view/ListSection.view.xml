<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
>
    <List
        headerText="{i18n>listSectionTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{guide>/Guides}"
    >
        <items>
            <ObjectListItem
                title="{guide>title}"/>
                
            <!-- 
                Failed Attempt on making a nested list
            <List
                headerText="{guide>title}"
                class="sapUiResponsiveMargin"
                width="auto"
                items="{guide>/Guides/0/tips}">
                <StandardListItem
                    title="{guide>attribute}"/>
            </List> -->
        </items>
    </List>
</mvc:View>